events {
    worker_connections 1024;# Complete E-Commerce Platform Setup Guide

## Prerequisites
- Node.js (v18 or higher)
- MongoDB (local or Atlas)
- Git
- Code editor (VS Code recommended)
- Docker (optional, for containerization)

## Phase 1: Project Structure & Initial Setup

### 1.1 Create Project Structure
```bash
mkdir ecommerce-platform
cd ecommerce-platform

# Create main directories
mkdir frontend backend shared
mkdir backend/{models,routes,middleware,controllers,utils,config}
mkdir frontend/{src,public}
mkdir shared/{types,constants}